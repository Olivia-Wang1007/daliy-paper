import Vue from 'vue';

export declare class EnButton extends Vue {
  /**
   * 按钮类型，可选值为 top、upload、setup或者不设置
   * @default top
   */
  type?: 'top' | 'upload' | 'setup';
  /**
   * 按钮大小，可选值为small、large、default或者不设置
   * @default default
   */
  size?: '' | 'small' | 'large' | 'default';
  /**
   * 设置按钮为禁用状态
   * @default false
   */
  disabled?: boolean;
  /**
   * 设置按钮为加载中状态
   * @default false
   */
  loading?: boolean;
}