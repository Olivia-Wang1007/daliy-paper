import Vue, { VNode } from 'vue';

export declare class Timeline extends Vue {
  /**
   * 指定是否最后一个节点为幽灵节点
   * @default false
   */
  pending?: boolean;
  /**
   * 	时间轴方向，可选值为 horizontal（水平） 和 vertical（垂直
   * @default vertical
   */
  'mode'?: string;
}

export declare class TimelineItem extends Vue {
  /**
   * 圆圈颜色，可选值为blue、red、green，或自定义色值
   * @default blue
   */
  color?: 'blue' | 'red' | 'green' | string;
  /**
   * 偏移量
   * @default 6
   */
  'default-pop-offset'?: number;
  /**
   * 默认位置，可选值为['top', 'top-start', 'top-end', 'bottom', 'bottom-start', 'bottom-end', 'left', 'left-start', 'left-end', 'right', 'right-start', 'right-end']
   * @default top
   */
  'default-placement'?: string;
  /**
   * 词包裹
   * @default false
   */
  'word-wrap'?: boolean;
  /**
   * padding
   * @default '''
   */
  'padding'?: string;
  /**
   * slot插槽对象
   */
  $slots: {
    /**
     * 基本内容
     */
    '': VNode[];
    /**
     * 自定义时间轴点内容
     */
    dot: VNode[];
  };
}
