import Vue from 'vue';

export declare class Button extends Vue {
  /**
   * 按钮类型，可选值为 default、primary、dashed、text、info、success、warning、error或者不设置
   * @default default
   */
  type?:
    | 'default'
    | 'primary'
    | 'secondary'
    | 'dashed'
    | 'text'
    | 'info'
    | 'success'
    | 'warning'
    | 'error';
  /**
   * 幽灵属性，使按钮背景透明
   * @default false
   */
  ghost?: boolean;
  /**
   * 按钮大小，可选值为small、large、default或者不设置
   * @default default
   */
  size?: 'small' | 'large' | 'default';
  /**
   * 按钮形状，可选值为circle、circle-outline或者不设置
   */
  shape?: 'circle' | 'circle-outline';
  /**
   * 开启后，按钮的长度为 100%
   * @default false
   */
  long?: boolean;
  /**
   * 设置button原生的type，可选值为button、submit、reset
   * @default button
   */
  'html-type'?: 'button' | 'submit' | 'reset';
  /**
   * 设置按钮为禁用状态
   * @default false
   */
  disabled?: boolean;
  /**
   * 设置按钮为加载中状态
   * @default false
   */
  loading?: boolean;
  /**
   * 设置按钮的图标类型
   */
  icon?: string;
  /**
   * 设置按钮的自定义图标
   */
  'custom-icon'?: string;
}
