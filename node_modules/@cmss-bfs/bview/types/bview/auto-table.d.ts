import Vue from 'vue';

export declare class AutoTable extends Vue {
  /**
   * 数据模型
   * @default []
   */
  'data-models': AutoTableDataModelItem[];
  /**
   * 输入的数据实体
   * @default []
   */
  datas: object[];
  /**
   * 是否需要选择项
   * @default false
   */
  'need-section'?: boolean;
  /**
   * 是否需要操作栏
   * @default true
   */
  'need-actions'?: boolean;
  /**
   * 是否需要自动操作配置
   * @default false
   */
  'use-auto-actions'?: boolean;
  /**
   * 自定义操作栏内容时，needActions和useAutoActions要设置为true
   * @default [{name: '修改',act: 'modify'},{name: '删除',act: 'delObj'}]
   */
  actions: AutoTableActionItem[];
  /**
   * 表格尺寸，可选值为 large、small、default 或者不填
   */
  size?: '' | 'default' | 'small' | 'large';
  /**
   * 是否显示斑马纹
   * @default true
   */
  'show-stripe'?: boolean;
  /**
   * 不需要显示的列
   * @default []
   */
  'exclude-list'?: Array<string>;
  /**
   * 选中Actions中某一项时触发
   * eventName:对应action中定义的act字段；
   * row: 当前被点击行的数据
   */
  $emit(eventName: any, row: any): this;
  /**
   * 设置needSection后，选中某一项时触发
   * sections：已选项datas中的数据
   */
  $emit(eventName: 'on-select', sections: object[]): this;
}

/**
 * key	字段key	String	必需
 * name	字段名称	String	必需
 * width	宽度，不填的话默认最大宽度为200，最小为60	Number	非必需
 * sortable	是否需要排序	Boolean	非必需
 * serialize	序列化方法	Function	非必需
 * filters	过滤,[{'label':'男','value':'male'},{'label':'女','value':'female'}]	Array	非必需
 * filterMultiple	是否允许多选过滤	Boolean	非必需
 * filterMethod	过滤所使用的方法，如果启用过滤，该方法则必须填写	filterMethod (value, row)	非必需
 * children	子列	Array
 */
export declare class AutoTableDataModelItem {
  key: string;
  name: string;
  width?: number;
  sortable?: boolean;
  serialize?: () => any;
  filters?: AutoTableFilterItem[];
  filterMultiple?: boolean;
  filterMethod?: (value?: any, row?: any) => any;
  children?: AutoTableDataModelItem[];
}

export declare class AutoTableFilterItem {
  label?: string;
  value?: string;
}

/**
 * 自定义操作栏元素
 * name: 操作名称
 * act: 操作事件名
 */
export declare class AutoTableActionItem {
  name: string;
  act: string;
}
