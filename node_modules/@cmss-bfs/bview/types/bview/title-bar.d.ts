import Vue from 'vue';

export declare class TitleBar extends Vue {
  /**
   * 标题，必填
   * @default '''
   */
  title: string;
  /**
   * 返回对应的vue-router path
   * @default ''
   */
  'back-path'?: string;
  /**
   * 返回对应的vue-router name
   * @default ''
   */
  'back-to'?: string;
  /**
   * 是否可以返回，默认返回
   * @default false
   */
  'default-back'?: boolean
}