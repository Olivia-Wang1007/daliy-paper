import Vue from 'vue';

export declare class LinkTo extends Vue {
  /**
   * 
   * @default {}
   */
  'data-model'?: object;
  /**
   * 指定LinkTo组件底层默认渲染成何种标签，默认渲染成a标签
   * @default 'a'
   */
  tag?: string;
  /**
   * 目标路由链接的query
   * @default {}
   */
  query?: object;
  /**
   * 目标路由链接的params
   * @default {}
   */
  params?: object;
}