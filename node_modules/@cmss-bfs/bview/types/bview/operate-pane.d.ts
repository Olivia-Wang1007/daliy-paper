import Vue, { VNode } from 'vue';

export declare class OperatePane extends Vue {
  /**
   * 是否需要填充页脚部分	
   * @default false
   */
  'need-footer'?: boolean;
  /**
   * slot插槽对象
   */
  $slots: {
    'header': VNode[];
    'content': VNode[];
    'footer': VNode[];
  }
}