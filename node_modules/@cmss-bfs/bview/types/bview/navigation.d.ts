import Vue, { VNode } from 'vue';

export declare class Navigation extends Vue {
  /**
   * 标题栏
   * @default 'PAAS运营管理平台'
   */
  title?: string;
  /**
   * 菜单数据(主要使用这个)
   * @default []
   */
  permissions?: NavPermission[];
  /**
   * 菜单数据(主要使用这个)
   * @default []
   */
  menus?: NavMenuItem[];
  /**
   * 是否为顶部导航
   * @default true
   */
  'is-top'?: boolean;
  /**
   * 顶部导航类型可选1或者2
   * @default 1
   */
  'nav-type'?: number;
  /**
   * 是否需要缩进，仅侧导航使用
   * @default false
   */
  'need-tuck'?: boolean;
  $slots: {
    /**
     * title处自定义LOGO
     */
    logo: VNode[];
    /**
     * 顶部导航，右侧可填充位置
     */
    'right-menu': VNode[];
  };
}

export declare class NavigationMenu extends Vue {
  /**
   * 权限的名称，全局唯一
   */
  name: string;
  /**
   * 当前菜单节点的Permission
   */
  content?: NavPermission;
  /**
   * 子节点
   * @default []
   */
  children?: NavigationMenu[];
}

export declare class SideNavBar extends Vue {
  /**
   * 侧边栏导航标题
   * @default 'PAAS运营管理平台'
   */
  title?: string;
  $slots: {
    logo: VNode[];
    content: VNode[];
  };
}

export declare class NavPane extends Navigation {
  /**
   * 标题栏
   * @default 'ABC运营管理平台'
   */
  title?: string;
  /**
   * 菜单数据(主要使用这个)
   * @default []
   */
  permissions?: NavPermission[];
  /**
   * 菜单数据(主要使用这个)
   * @default []
   */
  menus?: NavMenuItem[];
  /**
   * 顶部导航类型可选1或者2
   * @default 1
   */
  'nav-type'?: number;
  /**
   * 是否需要缩进，仅侧导航使用
   * @default false
   */
  'use-slide'?: boolean;
  /**
   * 宽度是否为自适应
   * @default false
   */
  'is-self-adaption'?: boolean;

  $slots: {
    /**
     * title处自定义LOGO
     */
    logo: VNode[];
    /**
     * 顶部导航，右侧可填充位置
     */
    'right-menu': VNode[];
    /**
     * 主要逻辑区域
     */
    content: VNode[];
  };
}

/**
 * id	-	Number
 * name	权限的名称，全局唯一	String
 * extra	是否为外链，默认为false，此项功能需要当前项目支持外链,高级功能	Boolean
 * parentName	父菜单name，菜单树由此项维持	String
 * lang1	菜单名称	String
 * sortCode	菜单顺序	Number
 */
export declare class NavPermission {
  id: number;
  name: string;
  extra?: boolean;
  parentName?: string;
  params?: string;
  lang1?: string;
  sortCode?: number;
}

/**
 * name	权限的名称，全局唯一	String
 * content	当前菜单节点的Permission	Object
 * children	子节点	Array
 */
export declare class NavMenuItem {
  name: string;
  content?: NavPermission;
  children?: NavMenuItem[];
}
