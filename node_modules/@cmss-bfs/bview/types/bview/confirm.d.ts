import Vue, { CreateElement } from 'vue';
export declare class ConfirmInstance extends Vue {
  /**
   * 消息
   * @param config ConfirmConfig为相关配置,string为待显示的内容
   */
  info(config?: ConfirmConfig | string): void;
  /**
   * 成功
   * @param config ConfirmConfig为相关配置,string为待显示的内容
   */
  success(config?: ConfirmConfig | string): void;
  /**
   * 警告
   * @param config ConfirmConfig为相关配置,string为待显示的内容
   */
  warning(config?: ConfirmConfig | string): void;
  /**
   * 错误
   * @param config ConfirmConfig为相关配置,string为待显示的内容
   */
  error(config?: ConfirmConfig | string): void;
  /**
   * 对话框
   * @param config ConfirmConfig为相关配置,string为待显示的内容
   */
  show(config?: ConfirmConfig | string): void;
}

export declare class ConfirmConfig {
  /**
   * 标题，主要内容
   * @default '提示'
   */
  title?: string;
  /**
   * 详细内容，可不传
   */
  content?: string;
  /**
   * 点击确认的回调
   */
  onOk?: () => void;
  /**
   * 点击取消的回调,当传入null时不显示取消按钮
   */
  onCancel?: () => void;

  /**
   * render函数，用来自定义弹框内容，此时title和content无效
   * @param h Render函数
   */
  contentRender?: (h?: CreateElement) => void;
  /**
   * 宽度，单位 px
   * @default 315
   */
  width?: number | string;
  /**
   * 取消按钮的文字
   * @default 取消
   */
  cancelText?: string;
  /**
   * 确定按钮的文字
   * @default 确定
   */
  okText?: string;
}

declare module 'vue/types/vue' {
  interface Vue {
    /**
     * 对话框
     */
    $Confirm: ConfirmInstance;
  }
}
